<p class="text-center align-middle user_name dt-user-avatar" style="background-color:<%= ColorX.with_google("#{user.lastname}#{user.name}") %>"><%= "#{(user.name[0] || "empty").capitalize}#{(user.lastname[0] || "empty").capitalize}" %></p>
